# Normalize line endings. Force LF for files that run inside Linux containers
# (shell scripts / Dockerfiles break with CRLF).
* text=auto eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
*.conf text eol=lf
